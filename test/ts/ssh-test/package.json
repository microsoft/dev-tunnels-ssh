{
	"name": "@microsoft/dev-tunnels-ssh-test",
	"description": "Tests for the SSH library for Dev Tunnels",
	"keywords": [
		"SSH"
	],
	"scripts": {
		"compile": "npm run -C ../../.. compile",
		"watch": "npm run -C ../../.. watch .",
		"test": "npm run -C ../../.. test"
	}
}
