{
	"name": "@microsoft/dev-tunnels-ssh-tcp",
	"description": "SSH TCP extensions library for Dev Tunnels",
	"repository":"https://github.com/microsoft/dev-tunnels-ssh.git",
	"keywords": [
		"SSH"
	],
	"author": "Microsoft",
	"license": "MIT",
	"scripts": {
		"compile": "npm run -C ../../.. compile",
		"eslint": "npm run -C ../../.. eslint",
		"eslint-fix": "npm run -C ../../.. eslint-fix",
		"watch": "npm run -C ../../.. watch .",
		"test": "npm run -C ../../.. test"
	},
	"dependencies": {
		"@microsoft/dev-tunnels-ssh": "^3.1.0"
	}
}
